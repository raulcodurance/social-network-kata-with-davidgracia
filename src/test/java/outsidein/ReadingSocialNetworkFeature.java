package outsidein;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.mockito.runners.MockitoJUnitRunner;

@RunWith(MockitoJUnitRunner.class)
public class ReadingSocialNetworkFeature {

    @Test
    public void
    user_can_view_timeline_of_another_user() {

       // readingAction.read(userId, anotherUserId);

       // verify(user).view(anotherUserTimeLine);
    }
}
